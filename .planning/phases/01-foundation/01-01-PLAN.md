---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/env.d.ts
  - src/styles/global.css
  - src/pages/index.astro
  - src/layouts/BaseLayout.astro
  - .gitignore
  - .env.example
  - .planning/PROJECT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `npm run build` completes without errors"
    - "Running `npm run dev` starts the local server on port 4321 with no errors"
    - "Tailwind v4 utility classes render correctly in the browser (no unstyled output)"
    - "Stack decision (Tailwind v4 + Sanity v3) is documented in PROJECT.md Key Decisions table"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config with Sanity + React + Tailwind Vite plugin wired"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 entry point with @import"
    - path: "src/env.d.ts"
      provides: "Type references for Astro and @sanity/astro module"
    - path: "src/pages/index.astro"
      provides: "Placeholder home page that builds successfully"
    - path: ".planning/PROJECT.md"
      provides: "Stack decision documented in Key Decisions table"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import in frontmatter"
      pattern: "import.*global\\.css"
    - from: "src/env.d.ts"
      to: "@sanity/astro/module"
      via: "triple-slash reference"
      pattern: "reference types.*@sanity/astro/module"
---

<objective>
Scaffold the Astro 5 project with all Phase 1 integrations installed and configured, and document the confirmed stack decision in PROJECT.md.

Purpose: Every subsequent plan builds on this scaffold. Getting integrations wired correctly at the start prevents the most common pitfalls (Tailwind classes not loading, Sanity client not found at build time).

Output: A buildable Astro 5 project with Tailwind v4 (Vite plugin), Sanity integration, and React adapter configured; a placeholder home page; and PROJECT.md updated with confirmed technology decisions.
</objective>

<execution_context>
@/Users/johnmitchell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnmitchell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project and install all Phase 1 integrations</name>
  <files>
    package.json
    astro.config.mjs
    tsconfig.json
    src/env.d.ts
    src/styles/global.css
    src/pages/index.astro
    src/layouts/BaseLayout.astro
    .gitignore
    .env.example
  </files>
  <action>
Run the following commands in sequence from /Users/johnmitchell/Desktop/my-project:

1. Scaffold Astro with minimal template into the current directory (it is already empty except for .planning/):
   `npm create astro@latest . -- --template minimal --no-install --no-git`
   Then: `npm install`

2. Add Sanity integration and React adapter (required for embedded Studio):
   `npx astro add @sanity/astro @astrojs/react --yes`

3. Add Tailwind v4 via the Vite plugin (NOT @astrojs/tailwind — that is deprecated):
   `npx astro add tailwind --yes`

4. Install the Sanity image URL builder:
   `npm install @sanity/image-url`

After installation, the astro.config.mjs must use this exact pattern — verify it matches or overwrite:

```typescript
import { defineConfig } from 'astro/config';
import sanity from '@sanity/astro';
import react from '@astrojs/react';
import tailwindcss from '@tailwindcss/vite';
import { loadEnv } from 'vite';

const { PUBLIC_SANITY_PROJECT_ID, PUBLIC_SANITY_DATASET } = loadEnv(
  process.env.NODE_ENV,
  process.cwd(),
  ''
);

export default defineConfig({
  vite: {
    plugins: [tailwindcss()],
  },
  integrations: [
    sanity({
      projectId: PUBLIC_SANITY_PROJECT_ID,
      dataset: PUBLIC_SANITY_DATASET,
      useCdn: false,
      apiVersion: '2025-01-28',
      studioBasePath: '/studio',
    }),
    react(),
  ],
});
```

Ensure src/styles/global.css contains ONLY:
```css
@import "tailwindcss";
```

Ensure src/env.d.ts contains:
```typescript
/// <reference types="astro/client" />
/// <reference types="@sanity/astro/module" />
```

Create src/layouts/BaseLayout.astro:
```astro
---
import '../styles/global.css';
const { title = 'Interactive Artists' } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
```

Replace src/pages/index.astro with a placeholder that uses BaseLayout:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Interactive Artists — Coming Soon">
  <main class="min-h-screen flex items-center justify-center bg-black text-white">
    <h1 class="text-4xl font-bold">Interactive Artists</h1>
  </main>
</BaseLayout>
```

Create .env.example (safe to commit — contains no secrets):
```
PUBLIC_SANITY_PROJECT_ID=your-project-id-here
PUBLIC_SANITY_DATASET=production
```

Ensure .gitignore includes `.env` and `.env.local` (not .env.example). The `npm create astro` scaffold usually handles this, but verify.

Do NOT delete the .planning/ directory or any of its contents.
  </action>
  <verify>
    <automated>cd /Users/johnmitchell/Desktop/my-project && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npm run build` exits with code 0
    - astro.config.mjs uses `@tailwindcss/vite` Vite plugin (not @astrojs/tailwind)
    - src/env.d.ts has both triple-slash references
    - src/styles/global.css contains `@import "tailwindcss"`
    - .env is in .gitignore; .env.example is committed
  </done>
</task>

<task type="auto">
  <name>Task 2: Document confirmed stack decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Update the Key Decisions table in .planning/PROJECT.md to document the two confirmed Phase 1 decisions. The research has already resolved both pre-flagged uncertainties from STATE.md.

Add or update these rows in the Key Decisions table:

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Tailwind v4 via @tailwindcss/vite | @astrojs/tailwind is officially deprecated for Tailwind v4; v4 Vite plugin is the current approach per official Astro docs (confirmed 2026-03-11) | Confirmed — using Tailwind v4 |
| Sanity v3 (not MDX) | Team manages images and structured content (Project, TeamMember, PressItem, ClientLogo) edited by non-developers; MDX is code-first and not appropriate for this content model | Confirmed — using Sanity v3 |
| Static output (no @astrojs/vercel adapter) | Portfolio site has no dynamic data needs; static builds are faster, cheaper, and score higher on Lighthouse; Vercel auto-detects Astro static output | Confirmed — static output |
| Embedded Sanity Studio at /studio | One deploy, one repo; simpler for small teams vs. separate Sanity project deploy | Confirmed — embedded Studio |

Preserve all existing content in PROJECT.md. Only update the Key Decisions section.
  </action>
  <verify>
    <automated>grep -q "Tailwind v4" /Users/johnmitchell/Desktop/my-project/.planning/PROJECT.md && grep -q "Sanity v3" /Users/johnmitchell/Desktop/my-project/.planning/PROJECT.md && echo "Stack decisions documented OK"</automated>
  </verify>
  <done>
    PROJECT.md Key Decisions table contains rows for Tailwind v4, Sanity v3, static output, and embedded Studio — each with rationale and confirmed outcome.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes with exit code 0
- `grep "tailwindcss" astro.config.mjs` shows the Vite plugin import (not @astrojs/tailwind)
- `grep "@sanity/astro/module" src/env.d.ts` returns a match
- `grep "Tailwind v4" .planning/PROJECT.md` returns a match
- Run `npm run dev` and visit http://localhost:4321 — placeholder page renders with "Interactive Artists" heading in white on black background, confirming Tailwind classes are active
</verification>

<success_criteria>
- `npm run build` completes without errors
- `npm run dev` starts server on port 4321 with no console errors
- Tailwind utility classes render (white text, black background on placeholder page)
- PROJECT.md documents confirmed stack decisions for Tailwind v4 and Sanity v3
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
