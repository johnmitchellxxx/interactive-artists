---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .env
autonomous: false

requirements: []

must_haves:
  truths:
    - "A Sanity project exists with project ID and dataset name known"
    - ".env file exists locally with PUBLIC_SANITY_PROJECT_ID and PUBLIC_SANITY_DATASET set to real values"
    - "`npm run build` passes with real Sanity credentials in place"
  artifacts:
    - path: ".env"
      provides: "Local environment variables with real Sanity project ID and dataset"
  key_links:
    - from: ".env"
      to: "astro.config.mjs"
      via: "loadEnv() reading PUBLIC_SANITY_PROJECT_ID and PUBLIC_SANITY_DATASET"
      pattern: "PUBLIC_SANITY_PROJECT_ID"
---

<objective>
Provision a Sanity project and wire real credentials into the local environment so the Astro build can connect to Sanity.

Purpose: The Astro scaffold (Plan 01) and schemas (Plan 02) are code-complete but cannot connect to Sanity without a real project ID. This plan gates on human account access to create that project and copy credentials into .env.

Output: .env file with real Sanity credentials; Astro build confirmed with live Sanity connection.
</objective>

<execution_context>
@/Users/johnmitchell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnmitchell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Sanity project and configure local .env</name>
  <files>.env</files>
  <action>Human creates a Sanity project at sanity.io/manage (free tier), copies the Project ID and dataset name into a .env file at the project root, and runs npm run build to confirm the credentials are valid.</action>
  <what-automated>
    Plans 01 and 02 created the full Astro scaffold and all four Sanity schemas. The code is ready to connect to a live Sanity project — it just needs a real project ID and dataset name.
  </what-automated>
  <instructions>
**Step 1 — Create or locate your Sanity project:**

If you already have a Sanity account:
- Go to https://sanity.io/manage
- Either create a new project (click "New project", name it "Interactive Artists") or locate an existing project to use
- Note the **Project ID** (looks like: `abc12345`)

If you do not have a Sanity account:
- Go to https://sanity.io and click "Get started free"
- Create an account, then create a new project named "Interactive Artists"
- The free tier is sufficient for this project
- Note the **Project ID** shown after creation

**Step 2 — Note the dataset name:**

The default dataset is `production`. Use this unless you have a reason to change it.

**Step 3 — Create the .env file:**

In the project root (/Users/johnmitchell/Desktop/my-project), create a file named `.env` (no extension) with:

```
PUBLIC_SANITY_PROJECT_ID=your-actual-project-id
PUBLIC_SANITY_DATASET=production
```

Replace `your-actual-project-id` with the Project ID from Step 1.

**Important:** .env is already in .gitignore — it will not be committed. Never commit this file.

**Step 4 — Verify the build still passes:**

Run in your terminal:
```
cd /Users/johnmitchell/Desktop/my-project && npm run build
```

The build should complete without errors. If you see `PUBLIC_SANITY_PROJECT_ID is undefined`, double-check that the .env file has no extra spaces around the `=` sign.
  </instructions>
  <verify>
    <automated>test -f /Users/johnmitchell/Desktop/my-project/.env && grep -q "PUBLIC_SANITY_PROJECT_ID" /Users/johnmitchell/Desktop/my-project/.env && cd /Users/johnmitchell/Desktop/my-project && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>.env file exists with real PUBLIC_SANITY_PROJECT_ID and PUBLIC_SANITY_DATASET values; `npm run build` passes with the live credentials.</done>
  <resume-signal>Type "done" once the .env file is in place and `npm run build` passes, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
After human completes setup:
- `test -f /Users/johnmitchell/Desktop/my-project/.env && echo ".env exists"` returns ".env exists"
- `grep "PUBLIC_SANITY_PROJECT_ID" /Users/johnmitchell/Desktop/my-project/.env | grep -v "your-"` returns a line with a real ID (not the placeholder)
- `cd /Users/johnmitchell/Desktop/my-project && npm run build` exits with code 0
</verification>

<success_criteria>
- Sanity project created (free tier) with project ID noted
- .env file contains real PUBLIC_SANITY_PROJECT_ID and PUBLIC_SANITY_DATASET values
- `npm run build` passes with the real credentials in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template. Record the dataset name (e.g., "production") in the summary — the project ID itself should NOT be recorded in summaries (treat it as a secret even though it's technically public).
</output>
