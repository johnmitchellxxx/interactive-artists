---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/sanity/schemaTypes/project.ts
  - src/sanity/schemaTypes/teamMember.ts
  - src/sanity/schemaTypes/pressItem.ts
  - src/sanity/schemaTypes/clientLogo.ts
  - src/sanity/schemaTypes/index.ts
  - src/sanity/imageUrl.ts
  - src/sanity/queries.ts
  - sanity.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "`npm run build` still passes after schemas are added"
    - "Sanity Studio (at /studio) will load all 4 document types once credentials are available"
    - "The image URL utility is available as a named export for use in pages"
    - "GROQ queries for all 4 content types are centralized in one file"
  artifacts:
    - path: "src/sanity/schemaTypes/project.ts"
      provides: "Project document schema with title, slug, category, mainImage (hotspot), description"
      exports: ["project"]
    - path: "src/sanity/schemaTypes/teamMember.ts"
      provides: "TeamMember document schema with name, role, photo (hotspot), bio, order"
      exports: ["teamMember"]
    - path: "src/sanity/schemaTypes/pressItem.ts"
      provides: "PressItem document schema with publication, quote, url, date"
      exports: ["pressItem"]
    - path: "src/sanity/schemaTypes/clientLogo.ts"
      provides: "ClientLogo document schema with name, logo (no hotspot), order"
      exports: ["clientLogo"]
    - path: "src/sanity/schemaTypes/index.ts"
      provides: "Barrel export of all schema types"
      exports: ["schemaTypes"]
    - path: "src/sanity/imageUrl.ts"
      provides: "urlFor() helper wrapping @sanity/image-url builder"
      exports: ["urlFor"]
    - path: "src/sanity/queries.ts"
      provides: "GROQ query strings for all 4 content types"
      exports: ["projectsQuery", "teamQuery", "pressQuery", "logosQuery"]
    - path: "sanity.config.ts"
      provides: "Sanity Studio configuration referencing all 4 schemas"
  key_links:
    - from: "sanity.config.ts"
      to: "src/sanity/schemaTypes/index.ts"
      via: "schema.types import"
      pattern: "from.*schemaTypes"
    - from: "src/sanity/imageUrl.ts"
      to: "sanity:client"
      via: "virtual module import"
      pattern: "from 'sanity:client'"
---

<objective>
Define all four Sanity content schemas (Project, TeamMember, PressItem, ClientLogo), create the Sanity Studio configuration, and add a shared image URL utility and centralized GROQ queries.

Purpose: These schemas are the data contracts for all Phase 2 UI work. Getting them right now — with correct field names, types, and hotspot settings — prevents Phase 2 from needing to revisit the schema layer.

Output: 4 schema files, barrel export, sanity.config.ts, imageUrl.ts utility, centralized queries.ts — all wired together and buildable.
</objective>

<execution_context>
@/Users/johnmitchell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnmitchell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types produced by Plan 01 that this plan builds on -->

From astro.config.mjs (written by Plan 01):
- Sanity integration configured at studioBasePath: '/studio'
- useCdn: false, apiVersion: '2025-01-28'
- `sanity:client` virtual module is available via `import { sanityClient } from 'sanity:client'`

From src/env.d.ts (written by Plan 01):
- `/// <reference types="@sanity/astro/module" />` enables TypeScript for `sanity:client`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all four Sanity schema files and barrel export</name>
  <files>
    src/sanity/schemaTypes/project.ts
    src/sanity/schemaTypes/teamMember.ts
    src/sanity/schemaTypes/pressItem.ts
    src/sanity/schemaTypes/clientLogo.ts
    src/sanity/schemaTypes/index.ts
  </files>
  <action>
Create the directory `src/sanity/schemaTypes/` and write the following four files exactly as specified. Use `defineType` and `defineField` from 'sanity' throughout.

**src/sanity/schemaTypes/project.ts**
```typescript
import { defineField, defineType } from 'sanity';

export const project = defineType({
  name: 'project',
  title: 'Project',
  type: 'document',
  fields: [
    defineField({
      name: 'title',
      type: 'string',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'slug',
      type: 'slug',
      options: { source: 'title' },
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'category',
      type: 'string',
      options: {
        list: [
          { title: 'Installation', value: 'Installation' },
          { title: 'Public & Outdoor', value: 'Public & Outdoor' },
          { title: 'Corporate Events', value: 'Corporate Events' },
        ],
      },
    }),
    defineField({
      name: 'mainImage',
      type: 'image',
      options: { hotspot: true },
      fields: [
        defineField({
          name: 'alt',
          type: 'string',
          title: 'Alt text',
        }),
      ],
    }),
    defineField({
      name: 'description',
      type: 'text',
    }),
    defineField({
      name: 'order',
      type: 'number',
      title: 'Display order',
    }),
  ],
  orderings: [
    {
      title: 'Display Order',
      name: 'orderAsc',
      by: [{ field: 'order', direction: 'asc' }],
    },
  ],
});
```

**src/sanity/schemaTypes/teamMember.ts**
```typescript
import { defineField, defineType } from 'sanity';

export const teamMember = defineType({
  name: 'teamMember',
  title: 'Team Member',
  type: 'document',
  fields: [
    defineField({
      name: 'name',
      type: 'string',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'role',
      type: 'string',
    }),
    defineField({
      name: 'photo',
      type: 'image',
      options: { hotspot: true },
      fields: [
        defineField({
          name: 'alt',
          type: 'string',
          title: 'Alt text',
        }),
      ],
    }),
    defineField({
      name: 'bio',
      type: 'text',
    }),
    defineField({
      name: 'order',
      type: 'number',
      title: 'Display order',
    }),
  ],
  orderings: [
    {
      title: 'Display Order',
      name: 'orderAsc',
      by: [{ field: 'order', direction: 'asc' }],
    },
  ],
});
```

**src/sanity/schemaTypes/pressItem.ts**
```typescript
import { defineField, defineType } from 'sanity';

export const pressItem = defineType({
  name: 'pressItem',
  title: 'Press Item',
  type: 'document',
  fields: [
    defineField({
      name: 'publication',
      type: 'string',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'quote',
      type: 'text',
    }),
    defineField({
      name: 'url',
      type: 'url',
    }),
    defineField({
      name: 'date',
      type: 'date',
    }),
  ],
});
```

**src/sanity/schemaTypes/clientLogo.ts**
```typescript
import { defineField, defineType } from 'sanity';

export const clientLogo = defineType({
  name: 'clientLogo',
  title: 'Client Logo',
  type: 'document',
  fields: [
    defineField({
      name: 'name',
      type: 'string',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'logo',
      type: 'image',
      options: { hotspot: false },
      fields: [
        defineField({
          name: 'alt',
          type: 'string',
          title: 'Alt text',
        }),
      ],
    }),
    defineField({
      name: 'order',
      type: 'number',
      title: 'Display order',
    }),
  ],
  orderings: [
    {
      title: 'Display Order',
      name: 'orderAsc',
      by: [{ field: 'order', direction: 'asc' }],
    },
  ],
});
```

**src/sanity/schemaTypes/index.ts**
```typescript
import { project } from './project';
import { teamMember } from './teamMember';
import { pressItem } from './pressItem';
import { clientLogo } from './clientLogo';

export const schemaTypes = [project, teamMember, pressItem, clientLogo];
```
  </action>
  <verify>
    <automated>cd /Users/johnmitchell/Desktop/my-project && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 5 files exist under src/sanity/schemaTypes/
    - Each schema uses defineType + defineField from 'sanity'
    - Image fields with hotspot: true on project.mainImage and teamMember.photo
    - clientLogo.logo has hotspot: false
    - All slug fields have `options: { source: 'title' }`
    - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sanity.config.ts, imageUrl utility, and centralized GROQ queries</name>
  <files>
    sanity.config.ts
    src/sanity/imageUrl.ts
    src/sanity/queries.ts
  </files>
  <action>
Write three files that complete the Sanity layer.

**sanity.config.ts** (project root — same level as astro.config.mjs):
```typescript
import { defineConfig } from 'sanity';
import { structureTool } from 'sanity/structure';
import { schemaTypes } from './src/sanity/schemaTypes/index';

export default defineConfig({
  name: 'default',
  title: 'Interactive Artists',
  projectId: process.env.PUBLIC_SANITY_PROJECT_ID!,
  dataset: process.env.PUBLIC_SANITY_DATASET ?? 'production',
  plugins: [structureTool()],
  schema: {
    types: schemaTypes,
  },
});
```

**src/sanity/imageUrl.ts**:
```typescript
import imageUrlBuilder from '@sanity/image-url';
import { sanityClient } from 'sanity:client';
import type { SanityImageSource } from '@sanity/image-url/lib/types/types';

const builder = imageUrlBuilder(sanityClient);

/**
 * Build a Sanity CDN image URL with format negotiation.
 * Usage: urlFor(image).width(800).auto('format').quality(75).url()
 *
 * NOTE: Use this for ALL Sanity-hosted images.
 * Do NOT pass Sanity image objects to Astro's <Image /> component.
 */
export function urlFor(source: SanityImageSource) {
  return builder.image(source);
}
```

**src/sanity/queries.ts**:
```typescript
/**
 * Centralized GROQ queries for all content types.
 * Import these in page frontmatter rather than writing inline GROQ strings.
 */

export const projectsQuery = `*[_type == "project"] | order(order asc) {
  _id,
  title,
  slug,
  category,
  description,
  mainImage { asset->, hotspot, crop, alt }
}`;

export const teamQuery = `*[_type == "teamMember"] | order(order asc) {
  _id,
  name,
  role,
  bio,
  photo { asset->, hotspot, crop, alt }
}`;

export const pressQuery = `*[_type == "pressItem"] | order(date desc) {
  _id,
  publication,
  quote,
  url,
  date
}`;

export const logosQuery = `*[_type == "clientLogo"] | order(order asc) {
  _id,
  name,
  logo { asset->, alt }
}`;
```
  </action>
  <verify>
    <automated>cd /Users/johnmitchell/Desktop/my-project && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - sanity.config.ts exists at project root and references schemaTypes barrel
    - src/sanity/imageUrl.ts exports urlFor() function typed with SanityImageSource
    - src/sanity/queries.ts exports 4 named query strings (projectsQuery, teamQuery, pressQuery, logosQuery)
    - `npm run build` passes
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes (exit code 0)
- `ls src/sanity/schemaTypes/` lists: project.ts, teamMember.ts, pressItem.ts, clientLogo.ts, index.ts
- `ls src/sanity/` lists: imageUrl.ts, queries.ts, schemaTypes/
- `ls sanity.config.ts` confirms file exists at project root
- `grep "hotspot: true" src/sanity/schemaTypes/project.ts` returns a match
- `grep "hotspot: true" src/sanity/schemaTypes/teamMember.ts` returns a match
</verification>

<success_criteria>
- All 4 Sanity document schemas defined with correct field names and types
- Image fields on project and teamMember have hotspot: true
- sanity.config.ts registers all 4 schema types
- urlFor() utility available for use in pages (Phase 2 dependency)
- GROQ queries centralized and named correctly for Phase 2 pages
- `npm run build` passes after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following the summary template.
</output>
